# VIDEO-TRANSCODER

> Generated by [create-omnidev-turbo](https://github.com/omnidev/create-omnidev-turbo)

A modern, fully-configured Turborepo monorepo with TypeScript, ESLint, Prettier, and Vitest.

## 📁 Structure

```
VIDEO-TRANSCODER/
├── apps/
│   └── backend/           # Backend application
├── packages/
│   ├── eslint-config/     # Shared ESLint configuration
│   ├── tsconfig/          # Shared TypeScript configuration
│   └── utils/             # Shared utilities
└── .github/
    └── workflows/         # CI/CD pipelines
```

## 🚀 Getting Started

### Prerequisites

- [Bun](https://bun.sh) v1.1.34 or higher

### Installation

```bash
# Install dependencies
bun install

# Start development
bun run dev
```

## 📜 Available Scripts

- `bun run dev` - Start all apps in development mode
- `bun run build` - Build all apps and packages
- `bun run test` - Run all tests
- `bun run test:watch` - Run tests in watch mode
- `bun run lint` - Lint all packages
- `bun run format` - Format all files with Prettier
- `bun run typecheck` - Type-check all packages
- `bun run clean` - Clean all build artifacts and dependencies

## 🏗️ Tech Stack

- **Build System**: [Turborepo](https://turbo.build)
- **Runtime**: [Bun](https://bun.sh)
- **Language**: [TypeScript](https://www.typescriptlang.org/)
- **Testing**: [Vitest](https://vitest.dev/)
- **Linting**: [ESLint](https://eslint.org/)
- **Formatting**: [Prettier](https://prettier.io/)
- **CI/CD**: GitHub Actions

## 📦 Packages

### `@repo/eslint-config`
Shared ESLint configuration with strict TypeScript rules.

### `@repo/tsconfig`
Shared TypeScript configuration for all packages.

### `@repo/utils`
Shared utility functions and helpers.

## 🐳 Docker Support

Each app includes a Dockerfile. Build and run with:

```bash
docker build -f apps/backend/Dockerfile -t VIDEO-TRANSCODER-backend .
docker run -p 3000:3000 VIDEO-TRANSCODER-backend
```

Or use docker-compose:

```bash
docker-compose up
```

## 📝 License

MIT
